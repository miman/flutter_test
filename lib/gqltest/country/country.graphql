query Country($code: ID!) {
  country(code: $code) {
    name
    native
    phone
    continent {
      code
      name
    }
    emoji
    currency
    languages {
      code
      name
    }
  }
}